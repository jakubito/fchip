{
  "name": "chip8-client",
  "private": true,
  "version": "0.0.1",
  "description": "CHIP-8 web client",
  "author": "Jakub Dobes <dobes.jakub@gmail.com>",
  "license": "ISC",
  "scripts": {
    "dev": "vite",
    "prebuild": "cd core && npm install && npm run build:release",
    "build": "tsc && VITE_APP_CORE_VERSION=`cd core && npm run get:version --silent` vite build",
    "postbuild": "cp core/build/core.wasm dist/core.wasm",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "tone": "^14.7.77"
  }
}
